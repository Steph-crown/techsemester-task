import { FC } from "react";
import { MarkdownEditorContainer } from "./markdown.style";

export const MarkdownEditor: FC = () => {
    return (
        <MarkdownEditorContainer>
            <h1></h1>
        </MarkdownEditorContainer>
    );
};
